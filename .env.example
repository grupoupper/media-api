# Caminho dentro do container (monte um volume aqui)
MEDIA_ROOT=/app/media

# Extensões permitidas
ALLOWED_MEDIA_EXT=mp4,webm,mov,m4v,avi,jpg,jpeg,png,webp,pdf,csv,xlsx,xls

# Limite de upload (MB)
MAX_UPLOAD_MB=400

# Token simples pra upload (Authorization: Bearer <UPLOAD_TOKEN>)
UPLOAD_TOKEN=troque-este-token

# URL pública (domínio apontado pro seu servidor do EasyPanel)
PUBLIC_BASE_URL=https://storage.grupoupper.com.br

# CORS (domínios do seu app na Vercel, separados por vírgula)
ALLOWED_ORIGINS=https://bi.grupoupper.com.br
